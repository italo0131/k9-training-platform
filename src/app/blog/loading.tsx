import RouteLoading from "@/app/components/RouteLoading"

export default function Loading() {
  return (
    <RouteLoading
      eyebrow="Carregando blog"
      title="Estamos puxando os posts, eventos e conversas mais relevantes da comunidade."
      accent="amber"
    />
  )
}
